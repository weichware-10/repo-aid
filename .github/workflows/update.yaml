name: update
on:
  push:
    branches:
      - main
jobs:
  update-util:
    runs-on: ubuntu-latest
    steps:
      - name: dispatch update
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: update
          repo: joshuajeschek/util
          token: ${{ secrets.WEICHWARE_UPDATE }}
          inputs: '{ "message": "${{ github.event.commits[0].message }}" }'
